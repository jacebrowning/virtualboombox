{
  "scripts": {
    "test-setup": "make clean doctor install",
    "test": "make test"
  },
  "env": {
    "GOOGLE_APPLICATION_CREDENTIALS": {
      "required": true
    },
    "GOOGLE_APPLICATION_CREDENTIALS_DATA": {
      "required": true
    },
    "SECRET_KEY": {
      "value": "test"
    }
  },
  "buildpacks": [
    {
      "url": "https://github.com/jacebrowning/heroku-buildpack-pipenv"
    }
  ],
  "addons": [
    "heroku-postgresql"
  ]
}
